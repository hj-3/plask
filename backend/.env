# backend/.env

# 서버 포트
PORT=3002

# DB 연결 문자열 (이게 없어서 에러가 났습니다!)
DATABASE_URL=postgresql://user:password@localhost:5432/enrollment

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
QUEUE_NAME=enrollment-queue

# JWT 시크릿 키 (로그인 토큰 생성용)
JWT_SECRET=plask-secret
JWT_EXPIRES_IN=1h